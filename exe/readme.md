To convert the python code to exe file for run in any computer with widnows os:

install library:
pip install auto-py-to-exe

run command into cmd:
auto-py-to-exe

a gui application will open then chose the file "main.py" and run.
